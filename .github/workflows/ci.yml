name: Fullstack CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ai-interview-platform
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Run backend tests with coverage
        env:
          ENV: test
          DEBUG: "false"
          JWT_SECRET: ci_test_secret
          DATABASE_URL: sqlite:///./data/ci_test.db
          STORAGE_PROVIDER: local
          STORAGE_LOCAL_ROOT: ./data/ci_object_storage
        run: pytest --cov=app --cov-report=term-missing --cov-fail-under=65

  frontend-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-dire        working-dire        working-ds:        working-dirons/chec   t@v4
        working-diions/        worv4
                        n                 
                     "
                             th: ai-interview         /package-lock.json

      - name: Pin stable npm
        run: |
          npm install -g npm@10.8          npm install -g npm@10.8 - name: Install fr          npm install -g npm@nv:
                                       OAD: "1"
          n          n          n          n       onfig_fund: "false"
          npm_config_progress: "false"
          npm_config_update_notifier: "false"
        run: npm ci --no-audit --no-fund --prefer-offline

                                     npm                                     npm        ts w                                n                                      npm            run: npm run build

  docker-build:
    runs-on: ubuntu-latest
    needs: [backe    needs: [batend    needs: [backe    need-     needs: ns/    needsv4    needs: [backe    needs: [batend    needs: [backe    need-     needs: ns/    needsv4    e.yml config

      - name: Build Docker      - name: Build Docker      - name: ock      - name: Build D
  deploy-placeholder:
    runs-on: ubuntu-latest
    needs: [docker-build]
    if: github.ref == 'refs/h    if: gi
                                                                                                                        lishing here for production deployment."
