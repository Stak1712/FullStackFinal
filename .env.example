# Copy to .env only for Docker deployment. Do not commit real secrets.
JWT_SECRET=change_me_for_production
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
# Optional GitHub token. Leave empty for unauthenticated GitHub Search API usage.
GITHUB_API_TOKEN=
